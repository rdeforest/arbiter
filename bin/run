#!/usr/bin/perl

use strict;
use warnings;

=pod

Iterates a world...

=cut

use Getopt::Long;

# TODO: GetOptions(...)

my $world_dir = shift;

my $world = new Iterator(dir => $world_dir);

my $iterations = 1;

while ($iterations) {
    $world->iterate;
    $iterations--;
}

